# cp .env.example .env  (no commitear .env)
# Puertos publicados en el host. Usuario/clave MQTT van en mosquitto/config/passwd (script).

MQTT_PORT=1883
MQTT_WS_PORT=9001
WEB_UI_PORT=8080

# Referencia (el broker NO lee esto; solo para que anotes lo mismo que en ESP32/UI):
# MQTT_USER=hidro_mqtt
# MQTT_PASSWORD=define_una_clave_larga
