# Copiar a .env y ajustar. No commitear .env.
# docker compose carga .env automaticamente.

# Puertos publicados (cambiar si hay conflictos en el servidor)
MQTT_PORT=1883
MQTT_WS_PORT=9001
WEB_UI_PORT=8080
